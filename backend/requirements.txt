# Python dependencies for this project.
#
# LOCAL DEVELOPMENT — you only need these three to run the metadata
# seed script (backend/scripts/seed_data.py):
motor>=3.5.0
bcrypt>=4.1.3
python-dotenv>=1.0.1

# EMERGENT PREVIEW ONLY — the three deps below back the FastAPI proxy
# shim (backend/server.py). Delete server.py and remove these lines if
# you are running purely locally.
fastapi==0.110.1
uvicorn==0.25.0
httpx>=0.27.0
